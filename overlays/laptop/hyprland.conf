# Laptop overlay — Single display, power aware
monitor = eDP-1, 1920x1080@60, 0x0, 1

# Power saving
decoration {
  blur = false
  shadow = false
}

# Touchpad
input {
  touchpad {
    natural_scroll = true
    tap-to-click = true
    drag_lock = true
    disable_while_typing = true
  }
}
