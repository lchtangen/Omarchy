# VM overlay — Minimal resources, no GPU
monitor = Virtual-1, preferred, 0x0, 1

# Disable eye candy
decoration {
  blur = false
  shadow = false
  drop_shadow = false
}

animations {
  enabled = false
}

# Input for VM
input {
  kb_layout = us
  follow_mouse = 1
  accel_profile = flat
}
